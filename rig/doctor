#!/bin/sh
set -eu
kill -0 "$(head -1 "$DATA_DIR/server.pid")"
i=0
until curl --fail --silent "http://127.0.0.1:$PORT_BASE/health" >/dev/null; do
  i=$((i+1))
  if [ "$i" -ge 50 ]; then exit 1; fi
  sleep 0.1
done
